<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>7-Day Classic Safari Package | Travolo Tanzania</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Add the FinalParade-Serif font */
        @font-face {
            font-family: 'FinalParade-Serif';
            src: url('fonts/FinalParade-Serif.woff2') format('woff2'),
                 url('fonts/FinalParade-Serif.woff') format('woff');
            font-weight: normal;
            font-style: normal;
            font-display: swap;
        }

        /* Base Styles */
        :root {
            --primary: #2a5c45;
            --secondary: #e0b354;
            --accent: #d4a762;
            --dark: #1a1a1a;
            --light: #f8f8f8;
            --white: #ffffff;
            --text: #444444;
            --heading: 'FinalParade-Serif', serif; /* Updated to use FinalParade-Serif */
            --body: 'Poppins', sans-serif;
        }
        
        /* Rest of your CSS remains the same */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: var(--body);
            color: var(--text);
            line-height: 1.6;
            background-color: var(--light);
        }
        
        h1, h2, h3, h4, h5, h6 {
            font-family: var(--heading);
            color: var(--dark);
            margin-bottom: 15px;
            line-height: 1.3;
            font-weight: normal; /* Custom fonts often have their own weights */
        }
        
        /* ... rest of your existing CSS ... */
    </style>
    <!-- Add Poppins as fallback for body text -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
    <!-- Your HTML content remains the same -->
</body>
</html>